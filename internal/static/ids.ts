import {ids} from './lib/css.js';

export const [settingsTicker, invertID, adminHideLight, selectedLayerID, tokenSelector, psuedoLink, folderDragging, dragOver, folders, foldersItem, itemControl, imageIcon, layerLight, layerGrid, hiddenLayer, mapID, hideZoomSlider, zoomSliderID, zooming, lighting, mapMask, gridPattern] = ids(22),
cursors = ids(4);
